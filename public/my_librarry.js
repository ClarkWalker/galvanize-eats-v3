/**///2017-09-xx/////////////////////////////////////////////////////////////
function elName(element, i) {
  return document.getElementsByTagName(element)[i];
}
//**//////////////////////////////////////////////////////////////////////////
/**///2017-09-xx/////////////////////////////////////////////////////////////
function elClass(className, i) {
  return document.getElementsByClassName(className)[i];
}
//**//////////////////////////////////////////////////////////////////////////
/**///2017-09-xx/////////////////////////////////////////////////////////////
function elId(idName) {
  return document.getElementById(idName);
}
//**//////////////////////////////////////////////////////////////////////////
/**///2017-09-xx/////////////////////////////////////////////////////////////
function newAttribute(attribute, value, targetEl) {
  return targetEl.setAttribute(attribute, value);
}
//**//////////////////////////////////////////////////////////////////////////
/**///2017-09-xx/////////////////////////////////////////////////////////////
function newEl(element, content, destination, attribute, value) {
  element = document.createElement(element);
  if (typeof(value) === `string`) {
    value = value.split(` `).join(``);
  }
  element.setAttribute(attribute, value);
  element.innerHTML = content;
  return destination.append(element);
}
//**//////////////////////////////////////////////////////////////////////////
/**///2017-09-xx/////////////////////////////////////////////////////////////
function typeValidation(data, type) {
  console.log(`data=`,typeof(data),`&`,`type=`,typeof(type));
  if (type == `str`) {type = `string`;}
  if (type == `arr`) {type = `array`;}
  if (typeof(data) === type) { return true;}
  if (typeof(data) !== type) { return false;}
}
//**//////////////////////////////////////////////////////////////////////////
